<header class="navbar">
  <section class="navbar-section">
    <a href="<%= coins_path %>" class="navbar-brand mr-10">
      <span data-text="CoinDB" class="glitch">CoinDB</span>
    </a>
  </section>
  <section class="navbar-section">
    <div class="input-group input-inline">
      <% if current_user %>
        <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
      <% else %>
        <%= link_to 'Login', new_user_session_path %>
      <% end %>
    </div>
  </section>
</header>
